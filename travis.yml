language: node_js
node_js:
  - '10.15.3'
addons:
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable
cache:
  directories:
  - node_modules
branches:
  only:
  - master
install:
  - npm install -g @angular/cli
script:
  - ng test --watch=false
  - ng e2e
